# 安全说明

这个项目可能会处理用户的 B站登录态、cookies、下载路径和本地文件。请把这些内容视为敏感信息。

## 不应提交的内容

- cookies 文件
- 浏览器导出的登录态
- 含有 `SESSDATA`、`bili_jct`、`DedeUserID` 等字段的文本
- 个人下载目录中的视频、封面、字幕
- 含绝对路径或账号信息的调试日志

## 建议做法

- 在设置中只保存 cookies 文件路径，不保存 cookies 内容。
- 日志默认脱敏，避免写入完整 URL、cookies 或本机隐私路径。
- 示例数据使用 mock 内容，不使用真实账号数据。
