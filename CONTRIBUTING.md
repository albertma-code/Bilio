# 贡献指南

感谢关注这个项目。当前项目仍处于早期阶段，主要目标是先做出稳定可用的桌面版本。

## 开发约定

- 不提交 cookies、token、账号信息或下载后的媒体文件。
- 下载核心逻辑和界面逻辑尽量分层，方便以后替换前端或接入 Tauri command。
- 涉及真实下载时，优先使用用户本机配置，不把登录态写死在代码里。
- UI 文案使用简洁中文。

## 提交前检查

```bash
.venv/bin/yt-dlp --version
```

如果之后引入前端工程，需要补充对应的 lint、typecheck 和构建命令。
